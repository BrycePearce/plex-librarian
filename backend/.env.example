PORT=8080
PLEX_URL=http://localhost:32400
PLEX_TOKEN=your_plex_token_here
DB_PATH=./data/librarian.db

# Optional: set to require ?token=<value> on the webhook endpoint.
# Recommended if your server is reachable from the internet.
# PLEX_WEBHOOK_SECRET=
